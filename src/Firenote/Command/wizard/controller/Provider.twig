{% set controllerName = namespace|lower %}
<?php

namespace {{ rootNamespace }}\Controllers\{{ namespace }};

use Silex\Application;
use Silex\ControllerProviderInterface;

class Provider implements ControllerProviderInterface
{
    public function connect(Application $app)
    {
        $app['{{ controllerName }}.controller'] = $app->share(function() use($app) {
            return $app->initializeController(new Controller());
        });

        // creates a new controller based on the default route
        $controllers = $app['controllers_factory'];

        // Auto generated stub : modify it
        // $controllers->get('/', '{{ controllerName }}.controller:someAction')->bind('some_route');
        
        return $controllers;
    }
}